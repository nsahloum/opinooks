<%= form_for(@debat) do |f| %>
  <% if @debat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@debat.errors.count, "error") %> prohibited this debat from being saved:</h2>

      <ul>
      <% @debat.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
